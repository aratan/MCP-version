[
  {
    "name": "MCP_SUMA",
    "description": "Suma dos números",
    "aliases": ["add", "sum", "suma"],
    "parameters": {
      "type": "object",
      "properties": {
        "a": { "type": "integer" },
        "b": { "type": "integer" }
      },
      "required": ["a", "b"]
    }
  },
  {
    "name": "MCP_RESTA",
    "description": "Subtract two numbers",
    "aliases": ["subtract", "sub"],
    "parameters": {
      "type": "object",
      "properties": {
        "a": { "type": "integer", "description": "The first number" },
        "b": { "type": "integer", "description": "The second number" }
      },
      "required": ["a", "b"]
    },
    "param_maps": { "x": "a", "y": "b" }
  },
  {
    "name": "MCP_MULTIPLICACION",
    "description": "Multiply two numbers",
    "aliases": ["multiply", "multiplication", "mul"],
    "parameters": {
      "type": "object",
      "properties": {
        "a": { "type": "integer", "description": "The first number" },
        "b": { "type": "integer", "description": "The second number" }
      },
      "required": ["a", "b"]
    },
    "param_maps": { "x": "a", "y": "b" }
  },
  {
    "name": "MCP_DIVISION",
    "description": "Divide two numbers (integer division)",
    "aliases": ["divide", "division", "div"],
    "parameters": {
      "type": "object",
      "properties": {
        "a": { "type": "integer", "description": "The dividend" },
        "b": { "type": "integer", "description": "The divisor" }
      },
      "required": ["a", "b"]
    },
    "param_maps": { "x": "a", "y": "b" }
  },
  {
    "name": "READ_FILE",
    "description": "Reads the content of a file",
    "aliases": ["fs.readFileSync", "readFile", "read_file"],
    "parameters": {
      "type": "object",
      "properties": {
        "file_path": { "type": "string", "description": "Path to the file to read" }
      },
      "required": ["file_path"]
    },
    "param_maps": {
      "uri": "file_path",
      "path": "file_path"
    }
  }
]
